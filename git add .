# ========================================
# Git Add Commands for All Project Files
# ========================================

# METHOD 1: Add All Files at Once (Recommended)
# =============================================
git add .


# METHOD 2: Add Each File Individually
# =====================================

# Python Script Files
git add bot.py
git add downloader.py
git add helpers.py
git add config.py
git add database.py

# Documentation Files
git add README.md
git add ABOUT.md

# Configuration Files
git add requirements.txt
git add runtime.txt
git add .env.example

# Other Files (if you have them)
git add LICENSE
git add .gitignore
git add Dockerfile


# METHOD 3: Add Multiple Files Together
# ======================================
git add bot.py downloader.py helpers.py config.py database.py


# METHOD 4: Add by File Type
# ===========================

# Add all Python files
git add *.py

# Add all Markdown files
git add *.md

# Add all text files
git add *.txt


# ========================================
# COMPLETE WORKFLOW EXAMPLES
# ========================================

# Example 1: Add All Files
git add .
git commit -m "✨ Initial commit - Add all files"
git push


# Example 2: Add Only Python Files
git add *.py
git commit -m "🐍 Update Python scripts"
git push


# Example 3: Add Documentation Only
git add README.md ABOUT.md
git commit -m "📝 Update documentation"
git push


# Example 4: Add Specific Files
git add bot.py helpers.py
git commit -m "🐍 Update bot.py and helpers.py"
git push


# Example 5: Add Requirements
git add requirements.txt
git commit -m "📦 Update requirements.txt"
git push


# ========================================
# QUICK COMMANDS (Copy & Paste)
# ========================================

# Update everything
git add . && git commit -m "🚀 Update all project files" && git push

# Update Python files
git add *.py && git commit -m "🐍 Update Python scripts" && git push

# Update documentation
git add *.md && git commit -m "📝 Update documentation" && git push

# First time upload
git add . && git commit -m "🎉 Initial commit" && git push -u origin main


# ========================================
# CHECK WHAT WILL BE ADDED
# ========================================

# See all changed files before adding
git status

# See what's staged after git add
git status


# ========================================
# USEFUL COMMANDS
# ========================================

# Remove file from staging (undo git add)
git reset bot.py

# Remove all files from staging
git reset

# See differences in files
git diff

# See commit history
git log --oneline
